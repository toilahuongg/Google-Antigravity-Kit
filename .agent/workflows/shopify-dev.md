---
description: Environment check and dev server startup.
---

1.  **Check Environment**:
    *   Verify `shopify.app.toml` exists.
    *   Check if `SHOPIFY_API_KEY` is present in `.env` (if applicable).

2.  **Start Server**:
    *   // turbo
    *   Run `npm run dev` to start the local development server and tunnel.

3.  **Monitor**:
    *   Remind the user to keep the terminal open.
