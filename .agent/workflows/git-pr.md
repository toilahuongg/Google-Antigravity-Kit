---
description: Create a Git Pull Request
---

1. Push the current branch to the remote repository.
   - Command: `git push origin HEAD`
2. Create a pull request using GitHub CLI.
   - Command: `gh pr create --fill`
   - Alternatively, use `gh pr create --web` to open the PR creation page in your browser.
